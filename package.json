{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); fs.rmSync('package-lock.json',{force:true}); fs.rmSync('yarn.lock',{force:true}); if (!process.env.npm_config_user_agent?.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev": "corepack pnpm --filter @workspace/jan-setu-ai run dev",
    "build": "corepack pnpm --filter @workspace/jan-setu-ai run build",
    "typecheck": "corepack pnpm --filter @workspace/jan-setu-ai run typecheck"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
